A geofencing server that can run on a Mac (SDK 10.7 or above). When running, this would provide services to iOs clients. Point your Mac browser to http://127.0.0.1:<port the server is running on>

for a list of all services
